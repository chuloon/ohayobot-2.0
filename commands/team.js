module.exports = {
    name: 'team',
    description: 'Adds a player to a team, or creates it if there isn\'t a matching team already',
    usage: '<team-name>',
    execute(message, args) {
        console.log("args", args);
        //if team exists
        //add author to permission
        
        //else
        //create category
        //create text
        //create voice
        
        //add author to role

        //send welcome message
    }
}